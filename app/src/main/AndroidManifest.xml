<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dmdmax.goonj">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Goonj">
        <activity android:name=".screens.activities.SigninActivity"></activity>
        <activity android:name=".screens.activities.LoginActivity"></activity>
        <activity android:name=".screens.activities.GetStartedActivity" />
        <activity android:name=".screens.activities.GetStarted1Activity" />
        <activity android:name=".screens.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>